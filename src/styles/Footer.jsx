import styled from "styled-components";

const Bottom = styled.footer`
  background-color: var(--cor-destaque);
  padding: 24px;
`;

const TextFooter = styled.p`
  color: var(--cor-fundo);
  text-align: center;
  font-family: var(--fonte-secundária);
  font-weight: 600;
  font-size: 1.5rem;
`;

export { Bottom, TextFooter };
